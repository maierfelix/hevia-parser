import {
  Token,
  Types as Type,
  TokenList as TT
} from "../../../labels";

import Node from "../../../nodes";

import {
  getNameByLabel
} from "../../../utils";

export function emitProgram(ast) {
  //this.emitBlock(ast);
}