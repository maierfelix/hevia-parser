/**
 * Version
 * @type {String}
 */
export const VERSION = require("../package.json").version;

/**
 * Default type, if a function
 * has no type assigned
 * @type {String}
 */
export const FUNC_DEFAULT_TYPE = "Void";